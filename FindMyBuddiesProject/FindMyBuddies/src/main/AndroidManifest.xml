<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.gercho.findmybuddies"
    android:versionCode="1"
    android:versionName="1.0">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>

    <uses-sdk
        android:minSdkVersion="10"
        android:targetSdkVersion="19" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme">
        <activity
            android:name="com.gercho.findmybuddies.MainActivity"
            android:label="@string/app_name"
            android:uiOptions="splitActionBarWhenNarrow">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".FindMyBuddiesActivity" />

        <service android:name=".services.UserService">
            <intent-filter>
                <action android:name="com.gercho.action.START_USER_SERVICE" />
                <action android:name="com.gercho.action.STOP_USER_SERVICE" />
                <action android:name="com.gercho.action.LOGIN_USER_SERVICE" />
                <action android:name="com.gercho.action.REGISTER_USER_SERVICE" />
                <action android:name="com.gercho.action.LOGOUT_USER_SERVICE" />
            </intent-filter>
        </service>
    </application>

</manifest>
